services:
  calculator:
    build: .
    image: my-c-app:local
    stdin_open: true
    tty: true

  dev:
    build: .
    image: my-c-app:dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    entrypoint: ["/bin/bash"]
